autoware_system_design_format: 0.3.0

# node information
name: TrackedObjectSorter.node

package:
  name: autoware_object_sorter
  provider: autoware

launch:
  plugin: autoware::object_sorter::TrackedObjectSorterNode
  executable: tracked_object_sorter_node
  node_output: screen

# interfaces
subscribers:
  - name: objects
    message_type: autoware_perception_msgs/msg/TrackedObjects

publishers:
  - name: objects
    message_type: autoware_perception_msgs/msg/TrackedObjects
    qos:
      reliability: reliable
      durability: transient_local

# parameters
param_files:
  - name: param_path
    schema: schema/object_sorter.schema.json
    default: config/tracked_object_sorter.param.yaml

param_values: []

# processes
processes:
  - name: filter
    trigger_conditions:
      - on_input: objects
    outcomes:
      - to_output: objects
